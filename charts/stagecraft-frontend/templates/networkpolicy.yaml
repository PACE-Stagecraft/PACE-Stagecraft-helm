apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "stagecraft-frontend.fullname" . }}-netpol
  namespace: stagecraft
spec:
  podSelector:
    matchLabels:
      {{- include "stagecraft-frontend.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kgateway-system
      ports:
        - port: {{ .Values.service.port }}
  egress:
    - {}
