{{- if .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "stagecraft-webhook.fullname" . }}
  namespace: stagecraft
spec:
  parentRefs:
    - name: stagecraft-gateway
      namespace: kgateway-system
      sectionName: http
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /webhooks
      backendRefs:
        - name: {{ include "stagecraft-webhook.fullname" . }}
          port: {{ .Values.service.port }}
{{- end }}
