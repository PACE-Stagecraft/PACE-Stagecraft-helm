apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "agora-worker.fullname" . }}-consumer
  namespace: agora
  labels:
    {{- include "agora-worker.labels" . | nindent 4 }}
    app.kubernetes.io/component: sqs-consumer
spec:
  replicas: {{ .Values.consumer.replicaCount }}
  selector:
    matchLabels:
      {{- include "agora-worker.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: sqs-consumer
  template:
    metadata:
      labels:
        {{- include "agora-worker.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: sqs-consumer
    spec:
      serviceAccountName: {{ .Values.serviceAccount.name }}
      containers:
        - name: {{ .Chart.Name }}-consumer
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["python", "-m", "app.sqs_consumer"]
          {{- if .Values.externalSecrets.secretsManagerPath }}
          envFrom:
            - secretRef:
                name: {{ include "agora-worker.fullname" . }}-secrets
          {{- end }}
          env:
            - name: ENVIRONMENT
              value: {{ .Values.environment | quote }}
            {{- range $k, $v := .Values.env }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
          resources:
            {{- toYaml .Values.consumer.resources | nindent 12 }}
