apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "stagecraft-mcp-github.fullname" . }}-netpol
  namespace: stagecraft
spec:
  podSelector:
    matchLabels:
      {{- include "stagecraft-mcp-github.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: stagecraft
      ports:
        - port: {{ .Values.service.port }}
  egress:
    - {}
