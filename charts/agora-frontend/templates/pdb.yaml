{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "agora-frontend.fullname" . }}
  namespace: agora
  labels:
    {{- include "agora-frontend.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "agora-frontend.selectorLabels" . | nindent 6 }}
{{- end }}
