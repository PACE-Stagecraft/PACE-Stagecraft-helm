apiVersion: v1
kind: Service
metadata:
  name: {{ include "agora-frontend.fullname" . }}
  namespace: agora
  labels:
    {{- include "agora-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agora-frontend.selectorLabels" . | nindent 4 }}
