{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "agora-webhook.fullname" . }}
  namespace: agora
  labels:
    {{- include "agora-webhook.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "agora-webhook.selectorLabels" . | nindent 6 }}
{{- end }}
