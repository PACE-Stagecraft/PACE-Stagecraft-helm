{{- if gt (int .Values.replicaCount) 1 }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "agora-api.fullname" . }}
  namespace: agora
  labels:
    {{- include "agora-api.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "agora-api.selectorLabels" . | nindent 6 }}
{{- end }}
