apiVersion: v1
kind: Service
metadata:
  name: {{ include "agora-api.fullname" . }}
  namespace: agora
  labels:
    {{- include "agora-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agora-api.selectorLabels" . | nindent 4 }}
