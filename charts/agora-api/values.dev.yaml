image:
  repository: ""
  tag: "latest"

replicaCount: 1

environment: dev

externalSecrets:
  secretsManagerPath: agora/dev/api

ingress:
  hosts:
    - host: "api.dev.agora.example.com"
      paths:
        - path: /
          pathType: Prefix
